# Backend env template — copy to .env and fill values
# Atlas project: ecommerce-backend (Cluster0) — currently EMPTY of e-commerce data
# Mongoose will create the e-commerce collections on first save.

PORT=
NODE_ENV=
MONGO_URI=
JWT_SECRET=
BASE_URL=
